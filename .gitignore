__pycache__/
*.py[cod]
.DS_Store
.env
results.json
data/catalog.jsonl
data/releases/
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
*.log

# Release archive - re-downloadable, never commit (19MB)
catalog.jsonl.gz

# --- our additions ---
.venv/
venv/
results-fast.json
results_v*.json

# Ranking LLM weights (~1-3GB): fetched by scripts/fetch_llm.py at setup time.
# The two encoders under models/ ARE committed - they are the offline floor.
models/qwen/

# Trace dumps for the demo walkthrough
traces/
results-refactor-check.json
results-smoke.json

# Primary reranker (~573MB): fetched by scripts/fetch_models.py, never committed.
# final_evaluation_faq.md section 4 prefers documented download instructions for large assets.
models/ce-bge/
models/ce-l12/
